<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="milu.kiriu2010.milurssviewer.main.URLLstActivity">

    <!-- メインコンテンツ -->
    <FrameLayout
        android:id="@+id/frameURLLst"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <!--
        <fragment
            android:id="@+id/fragmentURLLst"
            android:name="milu.kiriu2010.milurssviewer.main.URLLstFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:layout="@layout/fragment_rss_urllst" />
            -->
    </FrameLayout>

    <!-- ドロワーコンテンツ -->
    <FrameLayout
        android:id="@+id/frameGenreLst"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?android:attr/colorBackground">

    </FrameLayout>
</android.support.v4.widget.DrawerLayout>
